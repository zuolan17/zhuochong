# JUJU 桌宠

一个可以在 Windows 桌面运行的像素桌宠。

## 在线预览

开启 GitHub Pages 后，可直接在浏览器查看桌宠动画。点击桌宠还会和你打招呼。

## 本地运行

1. 安装 Node.js。
2. 在项目目录运行 `npm install`。
3. 运行 `npm start`。

## 打包 Windows 程序

运行 `npm run dist`。

说明：网页预览只展示动画和互动效果；桌面悬浮、始终置顶等功能需要运行 Windows 程序。
